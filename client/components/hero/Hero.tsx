import React from "react";

const Hero = () => {
	return (
		<div className="flex justify-center w-full bg-[var(--hero)] items-center">
			{/* Text */}
			What are you looking for ?{/* search bar */}
			Seach with button
			{/* india mart seals */}
		</div>
	);
};

export default Hero;
